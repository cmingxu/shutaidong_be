<%= form_for(@taidong) do |f| %>
  <% if @taidong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taidong.errors.count, "error") %> prohibited this taidong from being saved:</h2>

      <ul>
      <% @taidong.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :happened_at %><br>
    <%= f.datetime_select :happened_at %>
  </div>
  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :batch_id %><br>
    <%= f.text_field :batch_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
